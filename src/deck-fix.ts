import { renderPokerCard } from "./card-view";
import { startingDeck } from "./data/cards";
import type { Card } from "./entities/types";
import type { Game } from "./game/Game";

type DeckGame = Game & {
  cardCollection?: Card[];
  nuzlockeActive?: boolean;
  nuzlockeRules?: string[];
};

const esc = (value:string) => value.replaceAll("&","&amp;").replaceAll("<","&lt;").replaceAll(">","&gt;").replaceAll('"',"&quot;");

function game():DeckGame|undefined {
  return (window as Window & { __devlikeGame?: DeckGame }).__devlikeGame;
}

const STARTER_DECKS_BY_DEVELOPER:Record<string,string[]>={"michele-rinaldi":["git","coffee","quick-fix","wireframe","ctrl-z"],"sara-bianchi":["git","ctrl-z","quick-fix","stack-overflow","terminal"],"kevin-moretti":["docker","terminal","git","cache","monitor"],"andrea-ferri":["git","script","docker","api-call","quick-fix"],"gabriele-conti":["coffee","prototype","git","stack-overflow","quick-fix"],"laura-neri":["git","monitor","stack-overflow","terminal","lint"],"marco-valli":["exploit","grep","git","cache","ctrl-z"],"elena-rossi":["wireframe","prototype","coffee","git","monitor"],"davide-fabbri":["freelance-hustle","git","coffee","quick-fix","ctrl-z"],"chiara-romani":["git","coffee","docker","quick-fix","grep"],"paolo-sereni":["git","ctrl-z","terminal","script","stack-overflow"],"francesca-lombardi":["docker","terminal","cache","git","stack-overflow"],"luca-gentili":["git","script","api-call","docker","ctrl-z"],"simone-pieri":["coffee","prototype","git","lint","quick-fix"],"valentina-gori":["git","monitor","terminal","lint","wireframe"],"tommaso-riva":["exploit","grep","cache","ctrl-z","terminal"],"beatrice-falchi":["wireframe","prototype","coffee","monitor","lint"],"nicola-de-santis":["freelance-hustle","git","quick-fix","script","stack-overflow"]};

function starterDeckForDeveloper(developerId:string):Card[]{const ids=STARTER_DECKS_BY_DEVELOPER[developerId]??["git","coffee","docker","stack-overflow","ctrl-z"];return ids.map(id=>startingDeck.find(card=>card.id===id)).filter((card):card is Card=>Boolean(card)).map(card=>({...card,tier:1}));}
function ensureCollection(g:DeckGame){if(!g.cardCollection)g.cardCollection=(g.deck??[]).map(card=>({...card}));}
function locked(g:DeckGame){return !!g.nuzlockeActive&&!!g.nuzlockeRules?.includes("deckLock");}
function removalLocked(g:DeckGame){return locked(g)||!!g.nuzlockeActive&&!!g.nuzlockeRules?.includes("noCardRemoval");}
function duplicateLocked(g:DeckGame,card:Card){return !!g.nuzlockeActive&&!!g.nuzlockeRules?.includes("noDuplicates")&&g.deck.some(owned=>owned.id===card.id);}
function canAddCard(g:DeckGame,card:Card,owned:number,used:number){return !locked(g)&&g.deck.length<20&&used<owned&&!duplicateLocked(g,card);}

function renderModal(){const g=game();if(!g)return;ensureCollection(g);const counts=new Map<string,{card:Card,owned:number,used:number}>();g.cardCollection!.forEach(card=>{const current=counts.get(card.id);if(current)current.owned++;else counts.set(card.id,{card,owned:1,used:0});});g.deck.forEach(card=>{const current=counts.get(card.id);if(current)current.used++;});const collection=[...counts.values()];const deckLocked=locked(g);const noRemoval=removalLocked(g);const deckCards=g.deck.map((card,index)=>`<article class="deck-card ${noRemoval||g.deck.length<=5?"disabled":""}"><div class="deck-card-head"><b>${esc(card.name)}</b><strong>${card.cost} ⚡</strong></div><div class="deck-card-meta"><span>TIER ${card.tier??1}</span><span>${esc(card.effect.type.toUpperCase())}</span></div><p>${esc(card.description)}</p><button type="button" data-deck-action="remove" data-deck-index="${index}" ${noRemoval||g.deck.length<=5?"disabled":""}>RIMUOVI DAL MAZZO</button></article>`).join("");const collectionCards=collection.map(({card,owned,used})=>{const canAdd=canAddCard(g,card,owned,used);const sourceIndex=g.cardCollection!.findIndex(item=>item.id===card.id);return `<article class="deck-card collection-card ${!canAdd?"disabled":""}"><div class="deck-card-head"><b>${esc(card.name)}</b><strong>${card.cost} ⚡</strong></div><div class="deck-card-meta"><span>TIER ${card.tier??1}</span><span>POSSEDUTE ${owned}</span><span>MAZZO ${used}</span></div><p>${esc(card.description)}</p><button type="button" data-deck-action="add" data-deck-index="${Math.max(0,sourceIndex)}" ${canAdd?"":"disabled"}>AGGIUNGI AL MAZZO</button></article>`;}).join("");const message=deckLocked?"NUZLOCKE: DECK LOCK ATTIVO — il mazzo non può essere modificato.":noRemoval?"NUZLOCKE: RIMOZIONE CARTE VIETATA.":g.nuzlockeActive&&g.nuzlockeRules?.includes("noDuplicates")?"NUZLOCKE: NO DOPPIONI — una carta già nel MAZZO non può essere aggiunta.":"Puoi costruire il mazzo tra un combattimento e l'altro. Minimo 5 · massimo 20 carte.";const overlay=document.createElement("div");overlay.className="deck-overlay";overlay.innerHTML=`<section class="deck-modal"><header class="deck-modal-header"><div><h2>MAZZO</h2><p>${message}</p></div><button type="button" class="deck-close" data-deck-action="close">×</button></header><div class="deck-summary"><div><b>MAZZO</b><strong>${g.deck.length}/20</strong></div><div><b>CARTE POSSEDUTE</b><strong>${g.cardCollection!.length}</strong></div><div><b>SPAZIO MINIMO</b><strong>5</strong></div></div><div class="deck-columns"><section><div class="deck-section-title"><h3>MAZZO ATTIVO</h3><span>${g.deck.length} CARTE</span></div><div class="deck-grid">${deckCards||"<p class=\"deck-empty\">Il mazzo è vuoto.</p>"}</div></section><section><div class="deck-section-title"><h3>CARTE POSSEDUTE</h3><span>${collection.length} TIPI · ${g.cardCollection!.length} COPIE</span></div><div class="deck-grid">${collectionCards||"<p class=\"deck-empty\">Nessuna carta posseduta.</p>"}</div></section></div><footer class="deck-modal-footer"><span>Le carte rimosse restano tra le carte possedute e possono essere reinserite.</span><button type="button" class="primary" data-deck-action="close">TORNA AL GIOCO</button></footer></section>`;document.body.appendChild(overlay);overlay.addEventListener("click",event=>{const target=(event.target as HTMLElement).closest<HTMLElement>("[data-deck-action]");if(!target)return;const action=target.dataset.deckAction;if(action==="close"){overlay.remove();return;}const index=Number(target.dataset.deckIndex);if(action==="remove"&&!noRemoval&&g.deck.length>5){const remover=(g as DeckGame & {removeCardFromDeck?:(index:number)=>boolean}).removeCardFromDeck;if(remover)remover(index);else g.deck.splice(index,1);}if(action==="add"){const card=g.cardCollection![index];if(card){const owned=g.cardCollection!.filter(item=>item.id===card.id).length;const used=g.deck.filter(item=>item.id===card.id).length;if(canAddCard(g,card,owned,used))g.deck.push({...card});}}overlay.remove();renderModal();});}

function renderStarterDeckPreview(developerId:string){const container=document.querySelector<HTMLElement>(".starter-deck-preview");if(!container)return;const g=game();const developer=g?.startingCandidates.find(dev=>dev.id===developerId);const cards=starterDeckForDeveloper(developerId);container.innerHTML=`<div class="starter-deck-panel"><div class="starter-deck-header"><div><span class="starter-deck-kicker">LOADOUT INIZIALE</span><h3>MAZZO BASE</h3><p>Il mazzo con cui <b>${developer?esc(developer.name):"questo Developer"}</b> entra nella commessa.</p></div><div class="starter-deck-count"><strong>${cards.length}</strong><span>TOOL<br>INIZIALI</span></div></div><div class="starter-deck-divider"><span>CARTE DI PARTENZA</span><i></i><small>OGNI TOOL HA UN RUOLO NELLA STRATEGIA</small></div><div class="starter-deck-cards starter-deck-full-cards">${cards.map((card,index)=>`<div class="starter-card-slot"><span class="starter-card-index">0${index+1}</span>${renderPokerCard(card)}</div>`).join("")}</div></div>`;}
function injectStarterDeckPreview(){const g=game();if(!g||g.screen!=="team")return;const grid=document.querySelector<HTMLElement>(".developer-choice-grid");if(!grid)return;document.querySelectorAll<HTMLElement>(".developer-choice .starter-deck-preview").forEach(el=>el.remove());let preview=document.querySelector<HTMLElement>(".starter-deck-preview");if(!preview){preview=document.createElement("div");preview.className="starter-deck-preview";grid.insertAdjacentElement("afterend",preview);}const selected=g.selectedStartingId;if(!selected){preview.style.display="none";preview.innerHTML="";preview.removeAttribute("data-developer");return;}if(preview.dataset.developer===selected&&preview.style.display!=="none")return;preview.style.display="block";preview.dataset.developer=selected;renderStarterDeckPreview(selected);}
function renderBackpackModal(){
  const g=game();
  if(!g)return;
  const team=g.equipmentTeam;
  const nuz=g.nuzlockeActive?g.nuzlockeRules??[]:[];
  const noBackpack=nuz.includes("noBackpack");
  const oneEquipment=nuz.includes("oneEquipment");
  const overlay=document.createElement("div");
  overlay.className="deck-overlay backpack-overlay";
  const bag=g.inventory.map((item,i)=>'<button type="button" class="backpack-modal-item '+(g.equipmentSource?.zone==="bag"&&g.equipmentSource.itemIndex===i?"selected":"")+'" data-backpack-action="bag" data-item="'+i+'" '+(noBackpack?"disabled":"")+'><b>'+esc(item.name)+'</b><span>+'+item.codeBonus+' COD · +'+item.debugBonus+' DEBUG</span><small>'+esc(item.description)+'</small></button>').join("");
  const limit=oneEquipment?1:2;
  const slots=team.map((dev,di)=>'<section class="backpack-modal-dev"><div class="backpack-modal-dev-head"><b>'+esc(dev.name)+'</b><span>'+esc(dev.role)+'</span></div><div class="backpack-modal-slots">'+Array.from({length:limit},(_,si)=>{const item=dev.items[si];return '<button type="button" class="backpack-modal-slot '+(item?"filled":"empty")+' '+(g.equipmentSource?.zone==="dev"&&g.equipmentSource.devIndex===di&&g.equipmentSource.itemIndex===si?"selected":"")+'" data-backpack-action="dev" data-dev="'+di+'" data-item="'+si+'"><span>SLOT '+(si+1)+'</span><b>'+(item?esc(item.name):"VUOTO")+'</b><small>'+(item?'+'+item.codeBonus+' COD · +'+item.debugBonus+' DEBUG':"Slot disponibile")+'</small></button>';}).join("")+'</div></section>').join("");
  const message=noBackpack?"NUZLOCKE: NO ZAINO — gli oggetti devono restare equipaggiati.":g.equipmentSource?"Oggetto selezionato: clicca un altro slot o ZAINO per spostarlo.":"Clicca un oggetto e poi una destinazione. Puoi spostare e scambiare gli oggetti tra i Developer.";
  overlay.innerHTML='<section class="deck-modal backpack-modal"><header class="deck-modal-header"><div><h2>ZAINO</h2><p>'+message+'</p></div><button type="button" class="deck-close" data-backpack-action="close">×</button></header><div class="deck-summary"><div><b>OGGETTI NELLO ZAINO</b><strong>'+g.inventory.length+'</strong></div><div><b>DEVELOPER</b><strong>'+team.length+'</strong></div><div><b>SLOT EQUIPAGGIAMENTO</b><strong>'+team.reduce((n,d)=>n+d.items.length,0)+' / '+(team.length*limit)+'</strong></div></div><div class="backpack-modal-body"><aside class="backpack-modal-bag"><div class="deck-section-title"><h3>ZAINO</h3><span>'+g.inventory.length+' OGGETTI</span></div><div class="backpack-modal-items">'+(bag||'<p class="deck-empty">ZAINO VUOTO</p>')+'</div></aside><section class="backpack-modal-team"><div class="deck-section-title"><h3>EQUIPAGGIAMENTO TEAM</h3><span>'+limit+' SLOT PER DEVELOPER</span></div>'+slots+'</section></div><footer class="deck-modal-footer"><span>'+message+'</span><button type="button" class="primary" data-backpack-action="close">TORNA AL GIOCO</button></footer></section>';
  document.body.appendChild(overlay);
  overlay.addEventListener("click",event=>{
    const target=(event.target as HTMLElement).closest<HTMLElement>("[data-backpack-action]");
    if(!target)return;
    const action=target.dataset.backpackAction;
    if(action==="close"){g.equipmentSource=null;overlay.remove();return;}
    const zone=action==="bag"?"bag":"dev";
    const item=Number(target.dataset.item??-1);
    const dev=Number(target.dataset.dev??-1);
    if(g.equipmentSource){
      if(noBackpack&&zone==="bag"){g.message="NUZLOCKE: ZAINO VIETATO.";overlay.remove();renderBackpackModal();return;}
      g.moveSelectedEquipment(zone,dev,item);
    }else if(zone==="bag"&&!noBackpack){
      g.selectEquipmentSource(zone,item,dev);
    }else if(zone==="dev"){
      g.selectEquipmentSource(zone,item,dev);
    }
    overlay.remove();renderBackpackModal();
  });
}
function injectButton(){const g=game();if(!g||g.screen!=="map")return;document.querySelectorAll<HTMLElement>('.header-right [data-action="open-equipment"]').forEach(el=>el.remove());const strip=document.querySelector<HTMLElement>(".team-strip");if(!strip)return;const parent=strip.parentElement;if(!parent)return;if(parent.querySelector<HTMLElement>(".team-controls"))return;const controls=document.createElement("div");controls.className="team-controls";controls.innerHTML=`<div class="team-controls-title">GESTIONE TEAM</div><div class="team-controls-buttons"><button type="button" class="equipment-button" data-open-backpack="true">ZAINO · ${g.inventory.length}</button><button type="button" class="equipment-button deck-open-button" data-open-deck="true">MAZZO · ${g.cardCollection?.length??g.deck.length}</button></div>`;parent.insertBefore(controls,strip);}

export function setupDeckFix(g:Game){const dg=g as DeckGame;const originalStart=g.start.bind(g);g.start=()=>{originalStart();ensureCollection(dg);};const originalConfirm=g.confirmStartingDeveloper.bind(g);g.confirmStartingDeveloper=()=>{originalConfirm();const selected=dg.selectedStartingId;if(selected)dg.deck=starterDeckForDeveloper(selected);dg.cardCollection=(dg.deck??[]).map(card=>({...card}));};const originalReward=g.chooseReward.bind(g);g.chooseReward=(index:number)=>{originalReward(index);ensureCollection(dg);};document.addEventListener("click",event=>{const backpack=(event.target as HTMLElement).closest<HTMLElement>("[data-open-backpack]");if(backpack){event.preventDefault();event.stopImmediatePropagation();renderBackpackModal();return;}},true);document.addEventListener("click",event=>{const target=(event.target as HTMLElement).closest<HTMLElement>("[data-open-deck]");if(target){event.preventDefault();event.stopPropagation();renderModal();return;}const choice=(event.target as HTMLElement).closest<HTMLElement>(".developer-choice[data-dev]");if(choice&&game()?.screen==="team"){const developerId=choice.dataset.dev;if(developerId)window.setTimeout(()=>{const current=game();if(current?.screen==="team"&&current.selectedStartingId===developerId)renderStarterDeckPreview(developerId);else if(current?.screen==="team")injectStarterDeckPreview();},0);}});const observer=new MutationObserver(()=>{injectStarterDeckPreview();injectButton();});observer.observe(document.querySelector("#app")??document.body,{childList:true,subtree:true});injectStarterDeckPreview();injectButton();}
